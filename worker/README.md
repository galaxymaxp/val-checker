# Worker history

This directory preserves the original separate-VPS scaffold from the Version
2.0 build specification. The Track C blocker is retired. Phase 6 runs through
the protected daily server cron path at 00:05 UTC, with one database-enforced
storefront attempt per allowlisted user and UTC rotation. It does not expose an
on-demand or user-triggered Riot request surface.
